DATA SEGMENT
	NUM DW 5698H
DATA ENDS

CODE SEGMENT
	ASSUME CS:CODE, DS:DATA
START:
	MOV AX, DATA
	MOV DS, AX
        
	MOV AX, NUM
	
	MOV BL, AL
	AND BL, 0F0H
	MOV CL, 4
	SHR BL, CL
	
	MOV DL, AH
	AND DL, 0F0H
	MOV CL, 4
    SHR DL, CL 
    	
	MOV CL, AH
	AND CL, 0FH
    
	AND AL, 0FH

	MOV AH, 4CH
	INT 21H
CODE ENDS
	END START