DATA SEGMENT 
    BUFF	DB   1, 2, 3, '123'
    EBUFF	DB   0
    L		EQU  EBUFF - BUFF

DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE DS:DATA
START:
    MOV AX, DATA
    MOV DS, AX

    MOV BX, L			;L的值应为6

    MOV AH, 04CH
    INT 21H

CODE ENDS
    END START